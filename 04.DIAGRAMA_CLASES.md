classDiagram
    direction LR
    %% Entidad Usuario
    class Usuario {
        +Long id
        +String email
        +String password
        +String nombre
        +Boolean esPremium
    }

    %% Entidad Lista
    class Lista {
        +Long id
        +String titulo
        +String descripcion
        +String icono
        +String categoria
        +Boolean publica
        +Integer numDescargas
    }

    %% Entidad Item
    class Item {
        +Long id
        +String nombre
        +Boolean completado
        +String seccion
    }

    %% Relaciones
    Usuario "1" --> "*" Lista : propietaria_de
    Lista "1" *-- "*" Item : contiene
